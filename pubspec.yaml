name: flutter_pose_detection
description: "Hardware-accelerated pose detection using MediaPipe PoseLandmarker with GPU/NPU acceleration. Detects 33 body landmarks."
version: 0.4.1-cog
homepage: https://github.com/Cognizaunce/flutter_pose_detection
repository: https://github.com/Cognizaunce/flutter_pose_detection
issue_tracker: https://github.com/Cognizaunce/flutter_pose_detection/issues

environment:
  sdk: ^3.11.0
  flutter: '>=3.41.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.example.npu_pose_detection
        pluginClass: NpuPoseDetectionPlugin
      ios:
        pluginClass: NpuPoseDetectionPlugin

topics:
  - pose-detection
  - ml
  - machine-learning
  - computer-vision
  - npu
